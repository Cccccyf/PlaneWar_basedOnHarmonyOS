package com.example.hit_the_plane.view;

import ohos.media.image.PixelMap;

public class ExplosionSprite extends Sprite {
    public ExplosionSprite(PixelMap bitmap, int screenWidth, int screenHeight) {
        super(bitmap, screenWidth, screenHeight);
    }
}
